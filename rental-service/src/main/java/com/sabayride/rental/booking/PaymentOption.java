package com.sabayride.rental.booking;

/**
 * Both settle identically — the shop receives the same net amount and the
 * platform the same fee either way. Making PAY_AT_SHOP equally profitable is
 * what removes a shop's incentive to take the booking off-platform.
 */
public enum PaymentOption { PAY_NOW, PAY_AT_SHOP }
